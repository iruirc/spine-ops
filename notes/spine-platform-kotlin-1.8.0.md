- the block the four testers share no longer carries the neutral half: the AAA form, the `methodName_condition_expectedResult` convention, test size, the "never mock" list and the generic quality gate now come from `spine-toolkit:test-authoring`, read once before the first test of a task. What stays is Kotlin's — the boundaries a project actually has (`@TempDir`, an in-memory H2, an injected `Clock`, the DI container, Android platform APIs), the state that survives a test (coroutine scopes, test dispatchers, overridden bindings), and one gate line core cannot carry
- a new paragraph on test doubles in Kotlin: MockK will generate one for anything, the class under test included, so on the JVM the boundary list is what stands between a test and a double over the behaviour it was meant to check. Where the project already uses another mechanism — Mockito with `mockito-kotlin`, a hand-written fake — follow what is there and add no second one
- `kotlin-reviewer`'s `### Testing Adequacy` keeps its heading and judges by `spine-toolkit:test-authoring` → `## Review`: an assertion that cannot fail, a double standing in for the behaviour under test, state crossing between tests, behaviour no test names, a test asserting more than one behaviour. Unlike the plan-quality findings of the neighbouring skills, a finding there may block
- `kotlin-server-tester`'s Spring row names `@MockitoBean`: `@MockBean` is deprecated from Spring Boot 3.4, and the row says so rather than dropping the name a reader remembers. Micronaut's own `@MockBean` is a different annotation of a different framework and is untouched
- the core floor moves to `>=2.6.0 <3`: those sections exist only from 2.6.0. `scripts/lint-core-refs.sh` cannot catch a floor that is too low here — it resolves skill names, and the skill itself is older — so a new guard reads `test-authoring` out of the core checkout at the declared floor and requires the four headings
- `tests/foundation/lib/test-discipline.test.bats` also holds the line the other way: no agent or skill restates the neutral prose, and every tester keeps naming its Kotlin boundaries. The identity guard in `test-frameworks.test.bats` drops `## Test Structure` from the shared list and lowers its vacuity threshold to the block's new size
