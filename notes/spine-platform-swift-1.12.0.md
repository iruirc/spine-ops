- the snippet gate compiles marked samples through SIL, so a sample that races under Swift 6 fails CI, not only one that does not type-check; 139 marked blocks now compile, and a floor keeps the count from shrinking silently
- `di-factory` is on Factory 3: `import FactoryKit`, `from: "3.0.0"`, `@MainActor` on the factory variable, and what `reset()` and `Scope.singleton.reset()` clear as the 3.0.3 sources do; the reviewer, tester and diagnostics agents repeat it
- `di-swinject` breaks a circular dependency in `initCompleted` with both sides in `.container` scope; `di-composition-root`'s manual container ends its cycle, and `di-module-assembly`'s container conforms to the facade it shows
- persistence: Realm is for existing projects only and SwiftData is chosen by baseline; the storage, file-protection and GRDB facts are corrected; the Core Data backup goes through `replacePersistentStore(at:withPersistentStoreFrom:type:)` and keeps the copy; the SwiftData plan uses `SchemaMigrationPlan`
- networking: a 401 refreshes the token only while the rejected one is still current, over a client without the auth middleware, and login sets `requiresAuth: false`; retries honour `Retry-After`; a cancelled request surfaces as `CancellationError`; Moya is for existing codebases only; the OpenAPI adapter returns DTOs
- `nav-deeplinks` takes a cold-start link from the scene's connection options and holds the route until the app is ready; the macOS, AASA cache and deferred-link facts are corrected
- `arch-tca` samples follow the dependency, clock and storage rules the skill states; every `arch-mvvm` binding approach builds under Swift 6, and approach 3 gains a SwiftUI variant for screens below iOS 17
- the `arch-swiftui-navigation`, `arch-clean`, `arch-mvi`, `arch-mvc`, `arch-viper` and `arch-coordinator` samples compile under Swift 6 and put their full blocks under the gate; the VIPER Presenter's `deinit` cancels its fetch, and the coordinator draws its tab icons from SF Symbols
- `error-architecture` lets cancellation through the mapping chain; `concurrency-architecture`'s fan-out and upload samples compile and two isolation facts are corrected; `release-ops`, `reactive-combine` and `reactive-rxswift` correct their distribution, accessibility and operator facts
- `swift-init` no longer offers Realm or Moya to a new project
