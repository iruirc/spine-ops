- the `architecture` axis names the architecture only — `MVVM`, `MVI`, `TCA`, `VIPER`, `Clean Architecture`, `MVC`; navigation follows `ui`: UIKit gets a coordinator, SwiftUI a router, and TCA carries its own
- **action needed** in `## Stack`: replace `- Architecture: MVVM+Coordinator` with `- Architecture: MVVM`; replace any stack name an older `architecture-choice` wrote there (`MVVM + Router`, `Hybrid: MVVM + Coordinator + Router`, `Clean Architecture + SPM modules`) with its catalog value; and delete the `<!-- Chosen … -->` comment and `Objection:` line it left under the block. Until then the axis is asked on every task that needs it; `/setup` rewrites `MVVM+Coordinator` itself only while migrating a pre-split or single-file config
- the `tests` axis knows `Swift Testing`, and `import ComposableArchitecture` resolves `architecture` to `TCA` without a question
- `architecture-choice` writes only catalog values into `## Stack`, and no longer a comment or an `Objection:` line there; `arch-clean` and `arch-mvvm` take the async approach from `## Stack` instead of asking
- `swift-init`: `--architecture=mvi`; `mvvm-coordinator` is still accepted and means `mvvm`; the default is `mvvm` on both platforms, a SwiftUI scaffold gets `Navigation/AppRouter.swift` instead of a coordinator, and interactive DI and baseline answers are spelled into `## Stack` through the same flag table
- `workspace-init --resume` no longer waits for a `.swift-init.done` marker nothing wrote: the project step is done once its config is on disk
- the reference guides of `di-factory`, `di-swinject`, `di-module-assembly`, `net-architecture`, `nav-deeplinks`, `persistence-architecture` and `persistence-migrations` now load in an installed plugin: the skill reads its guide from beside itself, one section at a time, instead of through an `rg` path from the plugin root that found nothing in a project. Each guide opens with `## Contents`, its load table names those sections verbatim, and it keeps only what its `SKILL.md` does not
- references from one skill to another's section name a section that exists, in the form `` `di-swinject` → "Scopes" ``
- Swift samples a skill marks `<!-- typecheck -->` are compiled under Swift 6 against the iOS SDK in CI
