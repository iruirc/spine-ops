- a native Codex manifest, `.codex-plugin/plugin.json`: Codex installs the plugin from a marketplace and loads its skills. What runs there is a subset — every knowledge skill, `workspace-add` and `workspace-docs-regen`. The nine agents and four commands stay Claude Code components, and `manifest`, `swift-setup` and `workspace-init`, which spine-toolkit or `swift-init` drive, are kept out of Codex's implicit skill list; spine-toolkit ships no Codex manifest, so nothing orchestrates tasks there. Claude Code loads the plugin as before
- the workspace skills resolve every template, script and library path from `<platform-root>`, the plugin root two directories above the skill, instead of `${CLAUDE_PLUGIN_ROOT}`, so they run on either host
- the workspace skills now say where the `ws*::` libraries live and that every command calling them sources them and loads `workspace.yml` itself, since each host starts a fresh shell per command. `workspace-add` loads the file again after editing it: validation reads the loaded copy, not the file, and a copy taken before the edit let a dependency cycle through
- `architecture-choice` ends with the skill list on a host without `swift-init` and `swift-architect`
- the two manifests carry one identity and one version, and the foundation suite fails when they differ
