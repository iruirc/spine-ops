- `stack-detect` no longer reads the content of a task's files before the per-axis chain runs: step 2 matches names only, the chain is links a-c (`Task.md ## 4. [Stack]` → `## Modules` → `## Stack`), and content is a pass of its own, reached only for the axes those three left open and for the conditional `path:` add-ons awaiting corroboration. A configured project reads no file at all at the head of a dispatch, and a later stage of the same task answers from the cache link a reads
- an axis enters `needed` through a `path:` row alone. A value pinned by an `import:`, `token:` or `file:` row no longer widens the set — what the skill's "Reading the rules" already claimed and the algorithm contradicted — so the resolved stack string and the `## 4. [Stack]` cache narrow to the axes a task's files actually flag
- a platform's `## Heuristics` table is unchanged: the same rows, the same conditional add-on, still corroborated rather than trusted. Nothing a platform author writes has to move
- the reference driver floor moves to `>=1.14.0 <2` with this release; nothing the platforms vendor moved, so none of them has to re-vendor
