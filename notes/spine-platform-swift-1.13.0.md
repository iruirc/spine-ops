- `workspace-init` no longer speaks the plugin's planning vocabulary: its Example/ question says the answer is only recorded in `workspace.yml`, the xcodegen pre-flight line names the `project:` block that needs it, and the meta-repo README it renders lists only commands that exist and names the annotated `workspace.yml` skeleton without a path the user cannot reach
- the README quotes the core range `plugin.json` declares, describes the manifest's `## Driver` table, lists every skill including `release-ops`, and says which skills list Russian triggers; tests keep the range, the table and the skill list in step. `/workspace-add` names its optional flags `--push`, `--name` and `--symlink`
- every reference to another skill's section is written `` `<skill>` → "<H2>" `` and is tested to name a heading that exists, whether the prose around it uses straight quotes or guillemets; `arch-tca` points at the `@spine-platform-swift:swift-security` agent
- `swift-init` scaffolds TCA when `--architecture=tca`, the user's answer or an existing `## Stack` chooses it, and still does not recommend it unprompted
- examples in `pkg-spm-design`, `di-module-assembly` and the persistence guides use neutral names; the persistence guide shows the silent write failure on Core Data, and the snippet gate now holds at least 140 marked blocks
- `arch-tca`, `arch-mvi` and `arch-mvvm` state OS criteria from the lowest supported baseline, iOS 16 and macOS 13
- `persistence-architecture` keeps Core Data test stores on SQLite at `/dev/null`, since the in-memory store type raises on batch requests; `persistence-migrations` says what the progressive chain's inference fallback hides, and its versioned-envelope payloads no longer share a name with the lazy-migration sample
