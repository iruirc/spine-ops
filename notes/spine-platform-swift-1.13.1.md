- the `CLAUDE.md` that `workspace-init` and `workspace-add` write into each package no longer claims that a `workspace-check` command fails commits breaking the archetype rules, and the meta-repo `CONTRIBUTING.md` says what is actually checked: validating `workspace.yml` rejects a `deps` entry outside a non-empty `allowed_deps`, and nothing checks imports; a test now fails when the plugin's text names a `workspace-*` command it does not ship
